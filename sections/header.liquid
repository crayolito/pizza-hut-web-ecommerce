<!-- Mantiene exactamente tu estructura HTML original -->
<!-- Solo quita los estilos inline y los reemplaza por clases -->
<header class="encabezado">
  <div class="contenedor-logo">
    <img
      src="{{ 'logo-pizza-hut.png' | asset_url }}"
      alt="Logo Pizza Hut"
      class="logo"
    >
  </div>

  <nav class="menu-navegacion">
    <div class="menu-item">
      <a href="/menu">
        <h4>Menú</h4>
      </a>
    </div>
    <div class="menu-item menu-hutcoins">
      <a href="/promociones">
        <h4>Hut Coins</h4>
        {% render 'icon-flecha-hutcoins' %}
      </a>
    </div>
    <div class="menu-item">
      <a href="/restaurantes">
        <h4>Sobre Nosotros</h4>
      </a>
    </div>
  </nav>

  <div class="contenedor-opciones">
    <div class="contenedor-buscador">
      {% render 'icon-search', class: 'icono-buscar' %}
      <input type="search" class="campo-busqueda" placeholder="Buscar...">
    </div>

    <div class="contenedor-carrito">
      {% render 'icon-carrito' %}
    </div>

    <div class="modo-entrega">
      <p class="texto-entrega-titulo">Selecciona tu</p>
      <div class="contenedor-metodo">
        <h4 class="texto-metodo">Método de entrega</h4>
        {% render 'icon-domicilio', class: 'icono-domicilio' %}
      </div>
    </div>

    <button class="boton-login">
      <h4 class="texto-login">Iniciar sesión</h4>
      {% render 'icon-search', class: 'icono-login' %}
    </button>
  </div>
</header>

<!-- Añadir esto a tu archivo theme.liquid para incluir el CSS -->
<!--
  {% comment %}
    {{ 'header-responsivo.css' | asset_url | stylesheet_tag }}
  {% endcomment %}
-->

<style>
  /* Estilos que mantienen exactamente tu diseño original */
  .encabezado {
    width: 100%;
    height: 90px; /* Mantenemos altura exacta en desktop */
    padding: 0 53px; /* Mantenemos padding exacto en desktop */
    background-color: #fff;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .contenedor-logo {
    height: 37px;
    width: 136px;
  }

  .logo {
    height: 100%;
    width: 100%;
    object-fit: contain;
  }

  .menu-navegacion {
    display: flex;
    align-items: center;
    gap: 29px; /* Mantenemos gap exacto */
    width: auto;
  }

  .menu-item a {
    text-decoration: none;
    color: inherit;
  }

  .menu-hutcoins a {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .contenedor-opciones {
    display: flex;
    align-items: center;
    gap: 27px; /* Mantenemos gap exacto */
  }

  .contenedor-buscador {
    width: 177px;
    height: 43px;
    gap: 8px;
    display: flex;
    align-items: center;
    border-radius: 4px;
    border: 1.5px solid #6e6e6e;
    background-color: #fff;
    padding: 14px;
  }

  .campo-busqueda {
    width: 100%;
    border: none;
    outline: none;
  }

  .contenedor-carrito {
    display: flex;
    align-items: center;
    justify-content: center;
    height: 36px;
    padding: 0px 27px;
    border-left: 1.5px solid #dedede;
    border-right: 1.5px solid #dedede;
  }

  .modo-entrega {
    display: flex;
    height: 36px;
    padding: 0px 27px 0px 0px;
    gap: 0px 8px;
    border-right: 1.5px solid #dedede;
    flex-direction: column;
    align-items: start;
    justify-content: center;
  }

  .texto-entrega-titulo {
    text-transform: uppercase;
    font-weight: 600;
    margin: 0;
  }

  .contenedor-metodo {
    display: flex;
    align-items: center;
    gap: 4px;
  }

  .texto-metodo {
    margin: 0;
  }

  .boton-login {
    border-radius: 4px;
    background: #c8102e;
    gap: 8px;
    padding: 8px 16px;
    display: flex;
    align-items: center;
    justify-content: center;
    height: 36px;
    border: none;
    cursor: pointer;
  }

  .texto-login {
    color: #fff;
    margin: 0;
  }

  /* SOLO CAMBIOS PARA TABLETS - MÍNIMOS */
  @media screen and (max-width: 992px) {
    .encabezado {
      padding: 0 30px; /* Único cambio: reducimos ligeramente el padding lateral */
    }

    /* Todos los demás elementos mantienen sus dimensiones exactas */
  }

  /* CAMBIOS PARA MÓVILES - DONDE SÍ SON NECESARIOS */
  @media screen and (max-width: 768px) {
    .encabezado {
      height: auto; /* Permitimos que crezca verticalmente */
      padding: 15px 20px;
      flex-wrap: wrap; /* Permitimos que los elementos salten a la siguiente línea */
    }

    .contenedor-logo {
      /* Centrar logo */
      margin: 0 auto 15px;
    }

    .menu-navegacion {
      width: 100%;
      justify-content: center;
      gap: 20px; /* Reducimos espacio entre elementos */
      margin-bottom: 15px;
    }

    .contenedor-opciones {
      width: 100%;
      justify-content: center;
      flex-wrap: wrap;
      gap: 15px;
    }

    .contenedor-buscador {
      width: 100%;
      max-width: 300px;
    }

    .contenedor-carrito,
    .modo-entrega {
      border: none; /* Eliminamos bordes verticales que no se ven bien en móvil */
      padding: 0 10px;
    }

    .boton-login {
      margin-top: 10px;
      width: 100%;
      max-width: 300px;
    }
  }

  /* SOLO PARA MÓVILES MUY PEQUEÑOS */
  @media screen and (max-width: 480px) {
    .menu-navegacion {
      flex-direction: column;
      gap: 10px;
    }
  }
</style>

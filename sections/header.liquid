{% style %}
  :root {
    --fuente-principal: 'League Gothic', sans-serif;
    --fuente-secundaria: 'Montserrat', sans-serif;
    --color-primario: #231f20;
    --color-borde: #dedede;
    --color-borde-oscuro: #969696;
  }

  /* Nota de calculos para el uso de clamps para responsividad en ves de usar los medias querys
    Width(ancho):
    Tablet y desktop pequeño: Reducir un  20%  del valor original
    Móvil: Reducir un  40%  del valor original

    Padding y Margin:
    Tablet y desktop pequeño: Reudcir un 25% del valor original
    Móvil: Reudcir un 50% del valor original

    Height(alto):
    Tablet y desktop pequeño: Reducir un 20% del valor original
    Móvil: Reducir un 35% del valor original

    Simplificado:
    clamp(min, min + (max - min) * (100vw - tamaño-mín) / (tamaño-máx - tamaño-mín), max)

    En nuestro caso:
    tamaño-mín = 320px (típico ancho mínimo móvil)
    tamaño-máx = 1440px (típico ancho máximo desktop)
  */
{% endstyle %}

<sticky-header>
  <header
    style="
      width: 100%;
      height: 300px;
      padding: 0 53px;
      background-color: #FFF;
      display: flex;
      justify-content: space-between;
      align-items: center;
    "
    class="encabezado"
  >
    <div
      style="
        height: 37px;
        width: 136px;
      "
      class="contenedor-logo"
    >
      <img
        style="
          height: 100%;
          width: 100%;
          object-fit: contain;
        "
        src="{{ 'logo-pizza-hut.png' | asset_url }}"
        alt="Logo Pizza Hut"
        class="logo"
      >
    </div>

    <nav
      style="
        display: flex;
        align-items: center;
        gap: 29px;
        width: auto;
      "
      class="menu-navegacion"
    >
      <div class="menu-item">
        <a href="/menu" class="enlace-menu">Menú</a>
      </div>
      <div class="menu-item menu-hutcoins">
        <a href="/promociones" class="enlace-hutcoins">
          Hut Coins
          {% render 'icon-flecha-hutcoins', class: 'icono-flecha' %}
        </a>
      </div>
      <div class="menu-item">
        <a href="/restaurantes" class="enlace-menu">Sobre Nosotros</a>
      </div>
    </nav>

    <div class="contenedor-opciones">
      <div class="contenedor-buscador">
        {% render 'icon-search', class: 'icono-buscar' %}
        <input type="search" class="campo-busqueda" placeholder="Buscar...">
      </div>

      <div class="contenedor-usuario">
        <div class="contenedor-carrito">
          {% render 'icon-carrito', class: 'icono-carrito' %}
        </div>

        <div class="modo-entrega">
          <p class="texto-entrega-titulo">Selecciona tu</p>
          <div class="contenedor-metodo">
            <h4 class="texto-metodo">Método de entrega</h4>
            {% render 'icon-domicilio', class: 'icono-domicilio' %}
          </div>
        </div>

        <button class="boton-login">
          <span class="texto-login">Iniciar sesión</span>
          {% render 'icon-search', class: 'icono-login' %}
        </button>
      </div>
    </div>
  </header>
</sticky-header>

<script>
  class HeaderPrincipal extends HTMLElement {}
  customElements.define('sticky-header', HeaderPrincipal);
</script>

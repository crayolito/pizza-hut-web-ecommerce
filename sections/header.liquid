<!-- Header Component -->
<sticky-header>
  <header class="encabezado">
    <!-- Logo Section -->
    <div class="contenedor-logo">
      <img
        src="{{ 'logo-pizza-hut.png' | asset_url }}"
        alt="Logo Pizza Hut"
        class="logo"
      >
    </div>

    <!-- Navigation Menu -->
    <nav class="menu-navegacion">
      <div class="menu-item">
        <a href="/menu">
          <h4>Menú</h4>
        </a>
      </div>
      <div class="menu-item menu-hutcoins">
        <a class="link-hutcoins" href="/promociones">
          <h4>Hut Coins</h4>
          {% render 'icon-flecha-hutcoins' %}
        </a>
      </div>
      <div class="menu-item">
        <a href="/restaurantes">
          <h4>Sobre Nosotros</h4>
        </a>
      </div>
    </nav>

    <!-- Options Container -->
    <div class="contenedor-opciones">
      <!-- Search Bar -->
      <div class="contenedor-buscador">
        {% render 'icon-search', class: 'icono-buscar' %}
        <input type="search" class="campo-busqueda" placeholder="Buscar...">
      </div>

      <!-- Shopping Cart -->
      <div class="contenedor-carrito">
        {% render 'icon-carrito' %}
      </div>

      <!-- Delivery Mode -->
      <div class="modo-entrega">
        <p class="texto-entrega-titulo">Selecciona tu</p>
        <div class="contenedor-metodo">
          <h4 class="texto-metodo">Método de entrega</h4>
          {% render 'icon-domicilio', class: 'icono-domicilio' %}
        </div>
      </div>

      <!-- Login Button -->
      <button class="boton-login">
        <h4 class="texto-login">Iniciar sesión</h4>
        {% render 'icon-search', class: 'icono-login' %}
      </button>
    </div>
  </header>
</sticky-header>

<!-- Custom Element Definition -->
<script>
  class HeaderPrincipal extends HTMLElement {}
  customElements.define('sticky-header', HeaderPrincipal);
</script>

<style>
  /* Variables globales */
  :root {
    --fuente-principal: 'League Gothic', sans-serif;
    --fuente-secundaria: 'Montserrat', sans-serif;
    --color-primario: #231f20;
    --color-borde: #dedede;
    --color-borde-oscuro: #969696;
    --padding-lateral: clamp(1rem, 4vw, 53px);
    --altura-header: clamp(70px, 9vh, 90px);
    --color-rojo: #c8102e;
  }

  /* Reset y estilos base */
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  /* Estilos de tipografía */
  h4 {
    font-family: var(--fuente-principal);
    font-size: 1.5rem;
    font-style: normal;
    font-weight: 400;
    line-height: 1.6875rem;
    letter-spacing: -0.03rem;
    text-transform: uppercase;
  }

  h3 {
    font-family: var(--fuente-principal);
    font-size: 2rem;
    font-style: normal;
    font-weight: 400;
    line-height: normal;
    letter-spacing: -0.04rem;
    text-transform: uppercase;
  }

  h2 {
    font-family: var(--fuente-principal);
    font-size: 2.25rem;
    font-style: normal;
    line-height: normal;
    text-transform: uppercase;
  }

  p {
    font-family: var(--fuente-secundaria);
    font-size: 1rem;
    font-style: normal;
    font-weight: 400;
    line-height: 1.6875rem;
  }

  /* Estilos de enlaces e inputs */
  a {
    text-decoration: none;
    color: inherit;
    cursor: pointer;
  }

  input {
    font-family: var(--fuente-secundaria);
    font-size: 1rem;
    line-height: 1.6875rem;
    margin: 0;
    padding: 0;
    border: none;
    outline: none;
    background: transparent;
    width: 100%;
  }

  /* Header Principal */
  .encabezado {
    width: 100%;
    height: var(--altura-header);
    padding: 0 var(--padding-lateral);
    background-color: #fff;
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: clamp(1rem, 2vw, 2rem);
  }

  /* Logo */
  .contenedor-logo {
    height: clamp(30px, 5vh, 37px);
    width: clamp(110px, 12vw, 136px);
    flex-shrink: 0;
  }

  .logo {
    height: 100%;
    width: 100%;
    object-fit: contain;
  }

  /* Menú de Navegación */
  .menu-navegacion {
    display: flex;
    align-items: center;
    gap: clamp(1rem, 2vw, 29px);
    flex: 1;
    justify-content: center;
    min-width: 0;
  }

  .menu-item {
    white-space: nowrap;
  }

  .link-hutcoins {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  /* Contenedor de Opciones */
  .contenedor-opciones {
    display: flex;
    align-items: center;
    gap: clamp(1rem, 2vw, 27px);
    flex-shrink: 1;
  }

  /* Buscador */
  .contenedor-buscador {
    width: clamp(150px, 15vw, 177px);
    height: clamp(36px, 5vh, 43px);
    gap: 8px;
    display: flex;
    align-items: center;
    border-radius: 4px;
    border: 1.5px solid #6e6e6e;
    background-color: #fff;
    padding: 14px;
  }

  /* Carrito */
  .contenedor-carrito {
    display: flex;
    align-items: center;
    justify-content: center;
    height: clamp(32px, 4vh, 36px);
    padding: 0 clamp(1rem, 2vw, 27px);
    border-left: 1.5px solid var(--color-borde);
    border-right: 1.5px solid var(--color-borde);
  }

  /* Modo de Entrega */
  .modo-entrega {
    display: flex;
    height: clamp(32px, 4vh, 36px);
    padding-right: clamp(1rem, 2vw, 27px);
    gap: 0 8px;
    border-right: 1.5px solid var(--color-borde);
    flex-direction: column;
    align-items: start;
    justify-content: center;
  }

  .texto-entrega-titulo {
    text-transform: uppercase;
    font-weight: 600;
  }

  .contenedor-metodo {
    display: flex;
    align-items: center;
    gap: 4px;
  }

  /* Botón de Login */
  .boton-login {
    border-radius: 4px;
    background: var(--color-rojo);
    gap: 8px;
    padding: 8px 16px;
    display: flex;
    align-items: center;
    justify-content: center;
    height: clamp(32px, 4vh, 36px);
    border: none;
    cursor: pointer;
  }

  .texto-login {
    color: #fff;
    white-space: nowrap;
  }

  /* Media Queries */
  @media screen and (max-width: 1200px) {
    .texto-entrega-titulo {
      font-size: 0.875rem;
    }

    .texto-metodo {
      font-size: 1.25rem;
    }
  }

  @media screen and (max-width: 1024px) {
    .contenedor-opciones {
      gap: clamp(0.5rem, 1vw, 1rem);
    }

    .contenedor-buscador {
      width: clamp(120px, 12vw, 150px);
    }
  }
</style>

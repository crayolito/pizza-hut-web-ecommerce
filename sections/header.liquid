<!-- Header Component -->
<sticky-header>
  <header class="encabezado">
    <!-- Logo Section -->
    <div class="contenedor-logo">
      <img
        src="{{ 'logo-pizza-hut.png' | asset_url }}"
        alt="Logo Pizza Hut"
        class="logo"
      >
    </div>

    <!-- Navigation Menu -->
    <nav class="menu-navegacion">
      <div class="menu-item">
        <a href="/menu">
          <h4>Menú</h4>
        </a>
      </div>
      <div class="menu-item menu-hutcoins">
        <a class="link-hutcoins" href="/promociones">
          <h4>Hut Coins</h4>
          {% render 'icon-flecha-hutcoins' %}
        </a>
      </div>
      <div class="menu-item">
        <a href="/restaurantes">
          <h4>Sobre Nosotros</h4>
        </a>
      </div>
    </nav>

    <!-- Options Container -->
    <div class="contenedor-opciones">
      <!-- Search Bar -->
      <div class="contenedor-buscador">
        {% render 'icon-search', class: 'icono-buscar' %}
        <input type="search" class="campo-busqueda" placeholder="Buscar...">
      </div>

      <!-- Shopping Cart -->
      <div class="contenedor-carrito">
        {% render 'icon-carrito' %}
      </div>

      <!-- Delivery Mode -->
      <div class="modo-entrega">
        <p class="texto-entrega-titulo">Selecciona tu</p>
        <div class="contenedor-metodo">
          <h4 class="texto-metodo">Método de entrega</h4>
          {% render 'icon-domicilio', class: 'icono-domicilio' %}
        </div>
      </div>

      <!-- Login Button -->
      <button class="boton-login">
        <h4 class="texto-login">Iniciar sesión</h4>
        {% render 'icon-search', class: 'icono-login' %}
      </button>
    </div>
  </header>
</sticky-header>

<!-- Custom Element Definition -->
<script>
  class HeaderPrincipal extends HTMLElement {}
  customElements.define('sticky-header', HeaderPrincipal);
</script>

<style>
  /* Variables fundamentales del sistema */
  :root {
    --fuente-principal: 'League Gothic', sans-serif;
    --fuente-secundaria: 'Montserrat', sans-serif;
    --color-primario: #231f20;
    --color-borde: #dedede;
    --color-rojo: #c8102e;

    /* Sistema de espaciado adaptativo */
    --base-unit: 0.25rem; /* 4px base */
    --header-padding: calc(var(--base-unit) * 13); /* 52px */

    /* Sistema de tamaños mínimos seguros */
    --min-header-height: 90px;
    --min-logo-width: 136px;
    --min-button-width: 120px;
    --min-search-width: 150px;
  }

  /* Reseteo básico para prevenir problemas de layout */
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    min-width: 0; /* Previene overflow en flexbox */
  }

  /* Header con dimensiones mínimas garantizadas */
  .encabezado {
    width: 100%;
    min-height: var(--min-header-height);
    padding: 0 max(16px, var(--header-padding));
    background-color: #fff;
    display: flex;
    align-items: center;
    gap: 2rem;
    position: relative;
    z-index: 100;
  }

  /* Logo con tamaño mínimo asegurado */
  .contenedor-logo {
    min-width: var(--min-logo-width);
    width: clamp(var(--min-logo-width), 12vw, 180px);
    height: clamp(37px, 4vh, 45px);
    flex-shrink: 0;
    display: flex;
    align-items: center;
  }

  .logo {
    width: 100%;
    height: 100%;
    object-fit: contain;
  }

  /* Navegación principal con mejor distribución */
  .menu-navegacion {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: clamp(1rem, 2vw, 2rem);
    margin: 0 auto;
    flex: 1;
    min-width: max-content; /* Previene compresión excesiva */
  }

  .menu-item {
    position: relative;
    padding: 0.5rem;
  }

  .menu-item h4 {
    font-family: var(--fuente-principal);
    font-size: clamp(1.25rem, 1.5vw, 1.5rem);
    white-space: nowrap;
    text-transform: uppercase;
    letter-spacing: -0.03em;
  }

  /* Contenedor de opciones con mejor manejo de espacio */
  .contenedor-opciones {
    display: flex;
    align-items: center;
    gap: clamp(1rem, 1.5vw, 1.5rem);
    flex-shrink: 0;
    margin-left: auto;
  }

  /* Buscador mejorado */
  .contenedor-buscador {
    min-width: var(--min-search-width);
    width: clamp(var(--min-search-width), 15vw, 200px);
    height: clamp(43px, 5vh, 50px);
    border: 1.5px solid #6e6e6e;
    border-radius: 4px;
    display: flex;
    align-items: center;
    padding: 0 1rem;
    gap: 0.5rem;
    background: #fff;
  }

  .campo-busqueda {
    width: 100%;
    font-family: var(--fuente-secundaria);
    font-size: 1rem;
    background: transparent;
    border: none;
    outline: none;
  }

  /* Carrito con bordes consistentes */
  .contenedor-carrito {
    padding: 0 clamp(1rem, 1.5vw, 1.5rem);
    height: clamp(36px, 4vh, 43px);
    border-left: 1.5px solid var(--color-borde);
    border-right: 1.5px solid var(--color-borde);
    display: flex;
    align-items: center;
    flex-shrink: 0;
  }

  /* Selector de entrega mejorado */
  .modo-entrega {
    padding-right: clamp(1rem, 1.5vw, 1.5rem);
    height: clamp(36px, 4vh, 43px);
    border-right: 1.5px solid var(--color-borde);
    display: flex;
    flex-direction: column;
    justify-content: center;
    flex-shrink: 0;
  }

  .texto-entrega-titulo {
    font-size: clamp(0.75rem, 1vw, 0.875rem);
    text-transform: uppercase;
    font-weight: 600;
    white-space: nowrap;
  }

  .contenedor-metodo {
    display: flex;
    align-items: center;
    gap: 0.25rem;
  }

  .texto-metodo {
    font-size: clamp(1rem, 1.25vw, 1.25rem);
    white-space: nowrap;
  }

  /* Botón de inicio de sesión mejorado */
  .boton-login {
    min-width: var(--min-button-width);
    height: clamp(36px, 4vh, 43px);
    padding: 0 1rem;
    background: var(--color-rojo);
    border: none;
    border-radius: 4px;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    flex-shrink: 0;
  }

  .texto-login {
    color: #fff;
    font-size: clamp(1rem, 1.25vw, 1.25rem);
    white-space: nowrap;
  }

  /* Ajustes específicos para diferentes tamaños */
  @media (max-width: 1280px) {
    .encabezado {
      padding: 0 max(12px, 3vw);
      gap: 1rem;
    }

    .menu-navegacion {
      gap: 1rem;
    }

    .contenedor-opciones {
      gap: 0.75rem;
    }
  }

  @media (max-width: 1024px) {
    .texto-entrega-titulo {
      font-size: 0.75rem;
    }

    .menu-item h4 {
      font-size: 1.25rem;
    }
  }

  /* Soporte para navegadores específicos */
  @supports (-webkit-touch-callout: none) {
    .encabezado {
      gap: max(1rem, 2vw); /* Mejor manejo de gap en Safari */
    }
  }
</style>
